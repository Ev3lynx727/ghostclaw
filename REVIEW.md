# Project Review: Updates & Changes (March 2026)

This document summarizes the recent architectural updates and fixes implemented in Ghostclaw.

## 🚀 New Features

### Strict Mode for Analyzer
- Added `--strict` flag to the `analyze` command.
- When enabled, the CLI treats adapter errors as fatal and exits with status code 1.
- Centralized error collection in the `CodebaseAnalyzer` via the `PluginRegistry`.

### Enhanced Error Reporting
- The `MarkdownFormatter` and `TerminalFormatter` now include a dedicated "Adapter Errors" section.
- This provides visibility into transient failures within specific architectural adapters.

## 🔧 Core Refactorings & Fixes

### QMD (Quantum Memory Database) Backend
- **Interface Alignment**: Refactored `QueryEngine` to ensure `diff_runs` and `knowledge_graph` outputs match the standard `MemoryStore` contract.
- **Initialization Reliability**: Fixed `sqlite3.OperationalError` by ensuring database directories are created recursively during lazy initialization in `BM25Search` and `ReportIndexer`.
- **Dependency Injection**: Updated `QMDMemoryStore` to properly propagate configuration for enhanced search, embedding backends, and the "AI-Buff" scheme.

### CLI & Cache Stats
- Fixed a bug where `--cache-stats` missed reporting entry counts due to case-sensitivity in string output.
- Improved cache metadata collection to include accurate directory paths.

## 🧪 Verification & Coverage

- **Unit Tests**: All **214 tests** are passing.
- **Code Coverage**: Reached **65%** overall codebase coverage.
- **Integration**: Verified full orchestration flow from CLI to AI synthesis with strict mode validation.

---
*Generated by Ghostclaw Review System*
