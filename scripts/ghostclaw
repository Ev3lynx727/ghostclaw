#!/usr/bin/env bash
# Convenience wrapper to run ghostclaw with workspace src prioritized
# Usage: ./scripts/ghostclaw [args...]

cd "$(dirname "$0")/.."
exec env PYTHONPATH="src:$PYTHONPATH" ghostclaw "$@"
